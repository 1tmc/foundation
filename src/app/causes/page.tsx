import Causes from "@/components/causes/causes";
import Wrapper from "@/layout/Wrapper";

export const metadata = {
   title: "Causes CILF- Foundation",
};
const index = () => {
   return (
      <Wrapper>
         <Causes />
      </Wrapper>
   )
}

export default index